"use client";import {useState} from "react";import {Heart,Star} from "lucide-react";export function LocationActions({stationId,businessId}:{stationId:string;businessId?:string}){const[message,setMessage]=useState("");async function favorite(){const r=await fetch("/api/favorites",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({stationId})});setMessage(r.ok?"Saved to favorites.":"Sign in as a customer to save.")}async function review(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(!businessId)return;const r=await fetch("/api/reviews",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...Object.fromEntries(new FormData(e.currentTarget)),businessId})});setMessage(r.ok?"Review published.":(await r.json()).error)}return <div className="space-y-5"><button onClick={favorite} className="btn btn-ghost"><Heart className="h-4 w-4"/>Save location</button>{businessId&&<form onSubmit={review} className="glass rounded-2xl p-5"><h3 className="font-serif text-xl">Share your experience</h3><select name="rating" className="field mt-4"><option value="5">5 — Exceptional</option><option value="4">4 — Very good</option><option value="3">3 — Good</option><option value="2">2 — Fair</option><option value="1">1 — Poor</option></select><textarea name="comment" className="field mt-3" rows={3} placeholder="Your review"/><button className="btn btn-gold mt-3"><Star className="h-4 w-4"/>Publish review</button></form>}{message&&<p className="text-sm text-gold">{message}</p>}</div>}
