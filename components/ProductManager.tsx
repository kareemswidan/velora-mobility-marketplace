"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {Plus} from "lucide-react";export function ProductManager(){const[open,setOpen]=useState(false),[error,setError]=useState(""),router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const r=await fetch("/api/admin/products",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(r.ok){setOpen(false);router.refresh()}else setError((await r.json()).error)}return <section className="mt-12"><div className="flex justify-between"><h2 className="font-serif text-3xl">Catalog management</h2><button onClick={()=>setOpen(!open)} className="btn btn-gold py-2 text-xs"><Plus className="h-4 w-4"/>New product</button></div>{open&&<form onSubmit={submit} className="glass mt-5 grid gap-3 rounded-2xl p-5 md:grid-cols-2"><input className="field" name="name" placeholder="Product name" required/><input className="field" name="category" placeholder="Category" required/><input className="field" name="price" type="number" min="0" step=".01" placeholder="Price" required/><input className="field" name="stock" type="number" min="0" placeholder="Stock" required/><input className="field md:col-span-2" name="imageUrl" placeholder="Uploaded image URL"/><textarea className="field md:col-span-2" name="description" placeholder="Description"/>{error&&<p className="text-red-300">{error}</p>}<button className="btn btn-gold md:col-span-2">Create product</button></form>}</section>}
