"use client";import {useState} from "react";import {motion} from "framer-motion";import {Building2,CheckCircle2,Loader2,MapPin,UserRound} from "lucide-react";
const steps=["Owner","Business","Location","Service"];
export function BusinessRegistrationForm(){const[step,setStep]=useState(0),[busy,setBusy]=useState(false),[done,setDone]=useState(false),[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(step<3){setStep(s=>s+1);return}setBusy(true);const r=await fetch("/api/business/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});const x=await r.json();setBusy(false);if(r.ok)setDone(true);else setError(x.error)}if(done)return <motion.div initial={{opacity:0,scale:.95}} animate={{opacity:1,scale:1}} className="glass rounded-[2rem] p-12 text-center"><CheckCircle2 className="mx-auto h-16 w-16 text-gold"/><h2 className="mt-6 font-serif text-4xl">Application received.</h2><p className="mx-auto mt-4 max-w-md text-white/45">Our platform team will review your business. It remains private until approved.</p></motion.div>;return <form onSubmit={submit} className="glass rounded-[2rem] p-6 md:p-8"><div className="mb-8 flex items-center justify-between">{steps.map((x,i)=><div key={x} className="flex flex-1 items-center"><span className={`grid h-9 w-9 shrink-0 place-items-center rounded-full text-xs ${i<=step?"bg-gold text-ink":"bg-white/5 text-white/30"}`}>{i+1}</span>{i<3&&<i className={`mx-2 h-px flex-1 ${i<step?"bg-gold":"bg-white/10"}`}/>}</div>)}</div><div className={step===0?"grid gap-4":"hidden"}><h2 className="font-serif text-3xl">Tell us about you</h2><input className="field" name="ownerName" placeholder="Full name" required={step===0}/><input className="field" name="email" type="email" placeholder="Business email" required={step===0}/><input className="field" name="password" type="password" minLength={8} placeholder="Create password" required={step===0}/></div><div className={step===1?"grid gap-4":"hidden"}><h2 className="font-serif text-3xl">Your business</h2><input className="field" name="businessName" placeholder="Business name" required={step===1}/><select className="field" name="type" required={step===1}><option value="GAS_STATION">Gas station</option><option value="CAR_WASH">Car wash</option><option value="SERVICE_CENTER">Service center</option></select><textarea className="field" name="description" rows={4} placeholder="Describe your business" required={step===1}/><input className="field" name="phone" placeholder="Business phone" required={step===1}/><input className="field" name="website" placeholder="Website (optional)"/></div><div className={step===2?"grid gap-4":"hidden"}><h2 className="font-serif text-3xl">First location</h2><input className="field" name="branchName" placeholder="Branch name (optional)"/><input className="field" name="address" placeholder="Street address" required={step===2}/><input className="field" name="city" placeholder="City" required={step===2}/><div className="grid grid-cols-2 gap-4"><input className="field" name="latitude" type="number" step="any" placeholder="Latitude" required={step===2}/><input className="field" name="longitude" type="number" step="any" placeholder="Longitude" required={step===2}/></div><p className="text-xs text-white/30">Tip: copy the coordinates from Google Maps or OpenStreetMap.</p></div><div className={step===3?"grid gap-4":"hidden"}><h2 className="font-serif text-3xl">First service</h2><input className="field" name="serviceName" placeholder="Service name" required={step===3}/><textarea className="field" name="serviceDescription" placeholder="Service description"/><div className="grid grid-cols-2 gap-4"><input className="field" name="servicePrice" type="number" min="0" step=".01" placeholder="Price" required={step===3}/><input className="field" name="duration" type="number" min="5" placeholder="Minutes"/></div></div>{error&&<p className="mt-4 rounded-xl bg-red-400/10 p-3 text-sm text-red-300">{error}</p>}<div className="mt-7 flex justify-between gap-3">{step>0&&<button type="button" onClick={()=>setStep(s=>s-1)} className="btn btn-ghost">Back</button>}<button disabled={busy} className="btn btn-gold ml-auto">{busy?<Loader2 className="h-4 w-4 animate-spin"/>:step===3?"Submit for review":"Continue"}</button></div></form>}
