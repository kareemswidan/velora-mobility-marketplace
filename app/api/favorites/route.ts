import {getSession} from "@/lib/auth";import {prisma} from "@/lib/prisma";import {NextResponse} from "next/server";export async function POST(r:Request){const s=await getSession();if(s?.role!=="CUSTOMER")return NextResponse.json({error:"Sign in required"},{status:401});const{stationId}=await r.json();return NextResponse.json(await prisma.favorite.upsert({where:{userId_stationId:{userId:s.id,stationId}},update:{},create:{userId:s.id,stationId}}))}export async function DELETE(r:Request){const s=await getSession();if(s?.role!=="CUSTOMER")return NextResponse.json({error:"Sign in required"},{status:401});const{stationId}=await r.json();await prisma.favorite.deleteMany({where:{userId:s.id,stationId}});return NextResponse.json({ok:true})}
