import {getSession} from "@/lib/auth";import {prisma} from "@/lib/prisma";import {NextResponse} from "next/server";export async function PATCH(r:Request){if((await getSession())?.role!=="ADMIN")return NextResponse.json({error:"Unauthorized"},{status:401});const{id,price}=await r.json(),n=Number(price);if(!id||!Number.isFinite(n)||n<=0)return NextResponse.json({error:"Invalid price"},{status:400});return NextResponse.json(await prisma.gasPrice.update({where:{id},data:{price:n}}))}
