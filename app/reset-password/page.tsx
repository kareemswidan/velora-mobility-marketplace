"use client";import {useSearchParams} from "next/navigation";import {useState} from "react";import Link from "next/link";export default function ResetPassword(){const token=useSearchParams().get("token"),[message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password:new FormData(e.currentTarget).get("password")})});setMessage(r.ok?"Password updated. You can sign in now.":(await r.json()).error)}return <section className="mx-auto min-h-[75vh] max-w-md px-5 py-20"><form onSubmit={submit} className="glass rounded-3xl p-8"><p className="eyebrow">Account recovery</p><h1 className="mt-3 font-serif text-4xl">Create a new password</h1><input className="field mt-7" name="password" type="password" minLength={8} placeholder="New password" required/><button className="btn btn-gold mt-4 w-full">Update password</button>{message&&<p className="mt-4 text-sm text-gold">{message}</p>}<Link className="mt-5 block text-center text-xs text-white/40" href="/login">Return to sign in</Link></form></section>}
