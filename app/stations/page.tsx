import {prisma} from "@/lib/prisma";import {ExploreClient} from "@/components/ExploreClient";export const dynamic="force-dynamic";export const metadata={title:"Explore locations",description:"Find verified gas stations, car washes and service centers around the world."};
export default async function Stations(){const rows=await prisma.station.findMany({where:{OR:[{businessId:null},{business:{status:"APPROVED"}}]},include:{business:{select:{name:true,type:true,services:{where:{active:true},select:{name:true,price:true}}}}},orderBy:[{country:"asc"},{city:"asc"}]});const places=rows.map(x=>({...x,latitude:Number(x.latitude),longitude:Number(x.longitude),business:x.business?{...x.business,services:x.business.services.map(s=>({...s,price:String(s.price)}))}:null}));return <section className="mx-auto min-h-screen max-w-7xl px-5 py-16 md:px-8"><p className="eyebrow">Global network</p><h1 className="display mt-5 text-6xl md:text-8xl">Find what moves you.</h1><p className="mb-12 mt-6 max-w-2xl leading-7 text-white/45">Discover verified energy and vehicle-care businesses, compare services and navigate with OpenStreetMap.</p><ExploreClient places={places}/></section>}
