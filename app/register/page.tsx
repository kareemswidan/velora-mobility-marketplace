"use client";import Link from "next/link";import {useRouter} from "next/navigation";import {useState} from "react";export default function Register(){const r=useRouter(),[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const x=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(x.ok){r.push("/account");r.refresh()}else setError((await x.json()).error)}return <section className="mx-auto min-h-screen max-w-md px-5 py-20"><form onSubmit={submit} className="glass rounded-[2rem] p-8"><p className="eyebrow">Customer account</p><h1 className="mt-3 font-serif text-4xl">Join Velora</h1><div className="mt-7 space-y-4"><input className="field" name="name" placeholder="Full name" required/><input className="field" name="email" type="email" placeholder="Email" required/><input className="field" name="password" type="password" minLength={8} placeholder="Password" required/>{error&&<p className="text-sm text-red-300">{error}</p>}<button className="btn btn-gold w-full">Create account</button></div><p className="mt-5 text-center text-xs text-white/35">Already registered? <Link className="text-gold" href="/login">Sign in</Link></p></form></section>}
