import {prisma} from "@/lib/prisma";import {StoreClient} from "@/components/StoreClient";export const dynamic="force-dynamic";export default async function Store(){const products=await prisma.product.findMany({orderBy:[{featured:"desc"},{name:"asc"}]});return <section className="mx-auto min-h-screen max-w-7xl px-5 py-16 md:px-8"><p className="eyebrow">Velora E-Mart</p><div className="mb-14 flex flex-col justify-between gap-5 md:flex-row md:items-end"><h1 className="display mt-5 text-6xl md:text-8xl">Curated for<br/><i className="text-sage">the journey.</i></h1><p className="max-w-sm leading-7 text-white/40">Considered refreshments and premium car-care essentials, selected for life in motion.</p></div><StoreClient products={products.map(p=>({...p,price:String(p.price)}))}/></section>}
